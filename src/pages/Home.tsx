
import React from 'react';
import HomeContent from '@/components/home/Home';

const HomePage = () => {
  // Direct render without any transitions for maximum speed
  return (
    <HomeContent />
  );
};

export default HomePage;
